-- Long-form notes from the digital rep about the ad's goal / plan,
-- editable on the Meta Ads Planner ad form (replaces the inline name
-- field with a click-to-edit modal title).
ALTER TABLE "MetaAdsPacerAd" ADD COLUMN "digitalDetails" TEXT;
